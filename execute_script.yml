- name: Ejecutar script 
  hosts: localhost
  tasks:
    - name: Ejecutar script que imprime "Hola"
      command: /bin/bash -c "source myenv/bin/activate && python /ansible/prueba.py > /ansible/prueba.log"
